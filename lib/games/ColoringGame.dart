import 'package:flutter/material.dart';

class ColoringGame extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Game page'),
      ),
      body: Text("Coloring game!"),
    );
  }
}